
require ${PN} autotools [ supported_autoconf=[ 2.5 ] supported_automake=[ 1.10 ] ]

SLOT="0"
PLATFORMS="~x86"

src_prepare() {
    #glib-gettextize --force --copy || die "glib-gettextize failed"
    autotools_src_prepare
    glib-gettextize --force --copy || die "glib-gettextize failed"
}
